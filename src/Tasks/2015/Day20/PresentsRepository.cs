namespace App.Tasks.Year2015.Day20
{
    public class PresentsRepository
    {
        public int GetLeastPresents(string input)
        {
            int leastPresents = int.Parse(input);
            return leastPresents;
        }
    }
}
